---
name: DurationInputMask
route: /
---

import { Playground, Props } from 'docz'
import DurationInputMask from './DurationInputMask'

# DurationInputMask

<Props of={DurationInputMask} />

## Basic usage

<Playground>
  <DurationInputMask value={61} />
</Playground>

## Handlers

#### onBlur `onBlur={ (parsedValue, maskedValue, rawValue) => ... }`

<Playground>
  <DurationInputMask
    onBlur={(parsedValue, maskedValue, rawValue) => {
      console.log('onBlur', parsedValue, maskedValue, rawValue);
    }}
    maskOnChange={false}
    value={61}
  />
</Playground>

#### onChange `onChange={ (parsedValue, maskedValue, rawValue) => ... }`

_N.B. If an `onChange` handler is provided and `maskOnChange={false}`
it is the responsibility of the calling code to ensure that the `value`
prop is updated with the new value_

<Playground>
  <DurationInputMask
    onChange={(parsedValue, maskedValue, rawValue) => {
      console.log('onChange', parsedValue, maskedValue, rawValue);
    }}
    value={61}
  />
</Playground>

#### onKeyDown `onKeyDown={ (event) => ... }`

<Playground>
  <DurationInputMask
    onKeyDown={(event) => {
      event.persist();
      console.log('onKeyDown', event);
    }}
    value={61}
  />
</Playground>

#### onKeyUp `onKeyUp={ (event) => ... }`

<Playground>
  <DurationInputMask
    onKeyDown={(event) => {
      event.persist();
      console.log('onKeyUp', event);
    }}
    value={61}
  />
</Playground>
