---
name: DurationInputMask
route: /
---

import { Playground, Props } from 'docz'
import DurationInputMask from './DurationInputMask'

# DurationInputMask

<Props of={DurationInputMask} />

## Basic usage

<Playground>
  <DurationInputMask value={61} />
</Playground>

## Handlers

#### handleBlur `handleBlur={ (parsedValue, maskedValue, rawValue) => ... }`

<Playground>
  <DurationInputMask
    handleBlur={(parsedValue, maskedValue, rawValue) => {
      console.log('handleBlur', parsedValue, maskedValue, rawValue);
    }}
    maskOnChange={false}
    value={61}
  />
</Playground>

#### handleChange `handleChange={ (parsedValue, maskedValue, rawValue) => ... }`

_N.B. If an `handleChange` handler is provided and `maskOnChange={false}`
it is the responsibility of the calling code to ensure that the `value`
prop is updated with the new value_

<Playground>
  <DurationInputMask
    handleChange={(parsedValue, maskedValue, rawValue) => {
      console.log('handleChange', parsedValue, maskedValue, rawValue);
    }}
    value={61}
  />
</Playground>

#### onKeyDown `onKeyDown={ (event) => ... }`

<Playground>
  <DurationInputMask
    onKeyDown={(event) => {
      event.persist();
      console.log('onKeyDown', event);
    }}
    value={61}
  />
</Playground>

#### onKeyUp `onKeyUp={ (event) => ... }`

<Playground>
  <DurationInputMask
    onKeyDown={(event) => {
      event.persist();
      console.log('onKeyUp', event);
    }}
    value={61}
  />
</Playground>
